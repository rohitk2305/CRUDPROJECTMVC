@model ProjectCRUDOperation.Models.Product

@{
    ViewData["Title"] = "Create New Product";
}

<h2>Create New Product</h2>

<!-- Display Success or Error Message -->
@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success">
        @TempData["SuccessMessage"]
    </div>
    <meta http-equiv="refresh" content="2; url=@Url.Action("Index", "Product")" />
}
else if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger">
        @TempData["ErrorMessage"]
    </div>
    <meta http-equiv="refresh" content="2; url=@Url.Action("Create", "Product")" />
}

<form asp-action="Create" method="post">
    <div class="form-group">
        <label for="ProductName">Product Name</label>
        <input type="text" class="form-control" id="ProductName" name="ProductName" value="@Model.ProductName" required />
    </div>

    <div class="form-group">
        <label for="CategoryId">Category</label>
        <select class="form-control" id="CategoryId" name="CategoryId" required>
            <option value="">Select a Category</option>
            @foreach (var category in ViewData["CategoryId"] as SelectList)
            {
                <option value="@category.Value">@category.Text</option>
            }
        </select>
    </div>
    <br />
    <button type="submit" class="btn btn-success">Create Product</button>
</form>
<br />

@* <script>
    // Check if Success or Error message exists
    window.onload = function () {
        var successMessage = document.getElementById("SuccessMessage");
        var errorMessage = document.getElementById("ErrorMessage");

        // If success message is present, set timeout for redirect
        if (successMessage) {
            setTimeout(function () {
                window.location.href = '@Url.Action("Index", "Product")'; // Redirect to Index action
            }, 2000); // Wait for 2 seconds before redirecting
        }

        // If error message is present, set timeout for redirect
        if (errorMessage) {
            setTimeout(function () {
                window.location.href = '@Url.Action("Create", "Product")'; // Redirect to Create page
            }, 2000); // Wait for 2 seconds before redirecting
        }
    };
</script> *@

@* <div>
    <a href="@Url.Action("Index", "Product")" class="btn btn-secondary">Back to Product List</a>
</div> *@
